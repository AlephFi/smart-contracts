MigrateAuthSigner_Unit_Test
├── when caller is not VAULT_FACTORY
│   └── it should revert with AccessControlUnauthorizedAccount
└── when caller is VAULT_FACTORY
    ├── when newAuthSigner is address(0)
    │   └── it should revert with InvalidAuthSignerAddress
    └── when newAuthSigner is not address(0)
        └── it should set newAuthSigner and emit AuthSignerMigrated event