Initialize_Unit_Test
├── when manager passed is address(0)
│   └── it should revert with InvalidInitializationParams
├── when operationsMultisig passed is address(0)
│   └── it should revert with InvalidInitializationParams
├── when oracle passed is address(0)
│   └── it should revert with InvalidInitializationParams
├── when guardian passed is address(0)
│   └── it should revert with InvalidInitializationParams
├── when authSigner passed is address(0)
│   └── it should revert with InvalidInitializationParams
├── when underlyingToken passed is address(0)
│   └── it should revert with InvalidInitializationParams
├── when custodian passed is address(0)
│   └── it should revert with InvalidInitializationParams
├── when feeRecipient passed is address(0)
│   └── it should revert with InvalidInitializationParams
├── when managementFee is greater than maxManagementFee
│   └── it should revert with InvalidInitializationParams
├── when performanceFee is greater than maxPerformanceFee
│   └── it should revert with InvalidInitializationParams
└── when all params are valid
    └── it should set the initialization params, grant roles and set pausable flows